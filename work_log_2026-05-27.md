# 2026-05-27 工作记录

## 目标

围绕 SL-ISP 项目中的主算法 `NR-RG-RHO-LNS`，根据小规模 exact 算例暴露出的 gap，继续分析主算法与 EDD、SPT、典型元启发式算法、exact 解之间的差异，并据此优化主算法。

本日工作的核心问题是：

- 主算法是否能吸收 GA 的优势；
- 主算法为什么在某些小算例上仍落后于 GA 或 exact；
- 如何将“服务 quota 保护”和“迟交作业 TT 损失”同时纳入派工决策；
- 如何避免简单粗暴地用 exact 兜底，同时保持主算法自身的创新性和求解优势。

## 已完成的主要代码工作

### 1. 新增两个典型元启发式 baseline

在 `sl_isp_rg_rho_lns/src/algorithms/baselines_fast.py` 中新增：

- `SAFast`：模拟退火算法；
- `GAFast`：遗传算法。

两个算法都复用现有 `FastSchedule`、事件驱动仿真和目标函数评估接口，保证和 EDD、SPT、主算法在同一目标函数下可比。

同时在 `sl_isp_rg_rho_lns/src/experiments/run_pilot_benchmark.py` 中接入：

- `sa_fast`
- `ga_fast`

并新增测试文件：

- `sl_isp_rg_rho_lns/tests/test_metaheuristic_baselines.py`

### 2. 从 GA 中抽取主算法可吸收的优势

实验发现，GA 在代表算例中优于旧主算法，主要不是因为“种群”本身，而是因为 order-crossover 带来的全局作业序列重组能力。

据此在主算法中新增了一个 GA-inspired 邻域：

- `_repair_by_sequence_crossover`

位置：

- `sl_isp_rg_rho_lns/src/algorithms/rg_rho_lns_fast.py`

该邻域的设计思想：

- 当前 schedule 提取一个父序列；
- 从 EDD、SPT、WSPT、RG、Cover、Service-efficiency 等结构中生成供体父序列；
- 使用 order-crossover 生成新序列；
- 用 SGS 解码为 `FastSchedule`；
- 作为 ALNS repair operator 接入主算法。

后续实验表明，该邻域在投影层面能改进解，但如果强行把 sequence-commit 作为执行承诺，可能破坏服务结构。因此最终设计为：

- sequence crossover 是主算法内部候选生成器；
- 是否采用其执行结果，由 no-regret rollout 决定；
- 不直接替代主算法的稳健派工逻辑。

### 3. 发现主算法的关键失败机制

通过对 exact、GA、旧主算法的执行过程逐步对比，发现代表算例中的主要差异在于早期派工前缀：

```text
Exact: J1, J6, J9, J5
GA:    J0, J5, J8, J9
NR:    J0, J6, J5, J4
```

旧主算法的问题是：

- 过度保护了某些服务冗余作业，例如 J4、J1；
- 过早放弃了已经无法准时但提前加工能显著减少 TT 的作业，例如 J9。

因此主算法不是单纯“搜索不够”，而是执行阶段的边际收益判断不够精确。

### 4. 引入 quota-aware service gain

新增函数：

- `_quota_marginal_service_quantity`

位置：

- `sl_isp_rg_rho_lns/src/algorithms/rg_rho_lns_fast.py`

作用：

判断某个作业是否真的会影响实体服务 quota。如果去掉该作业后，其他作业仍能满足服务阈值，则该作业的服务边际收益为 0 或较低。

这解决了旧主算法中的问题：

```text
不是所有准时作业都有服务收益；
只有会减少 shortfall 的作业才应被强保护。
```

### 5. 引入 tardiness opportunity cost

新增函数：

- `_tardiness_opportunity_gain`

位置：

- `sl_isp_rg_rho_lns/src/algorithms/rg_rho_lns_fast.py`

作用：

衡量“如果这个作业现在不做，而是继续等待一次，会额外增加多少 tardiness”。

这用于识别 J9 这类作业：

- 可能已经无法准时；
- 也可能无法帮助服务 quota；
- 但越早做，TT 损失越小。

该机制使主算法不再只关注“能否救服务”，而是同时考虑 TT 的机会成本。

### 6. 新增 sequence-evolution rollout 候选

新增内部候选类：

- `_SequenceEvolutionRollout`

位置：

- `sl_isp_rg_rho_lns/src/algorithms/rg_rho_lns_fast.py`

设计方式：

- 使用 quota-aware、tardiness-aware、RG-aware 的多个结构序列作为初始父代；
- 做轻量级序列进化；
- 用现有仿真器完整 rollout；
- 只有当完整仿真结果优于其他候选时才会被选择。

该设计避免了两个风险：

- 不直接把 exact 当兜底；
- 不强行采用 GA-like 序列，避免破坏服务 quota。

### 7. 更新实验工厂配置

在 `sl_isp_rg_rho_lns/src/experiments/run_pilot_benchmark.py` 中新增/透传参数：

- `use_sequence_crossover`
- `sequence_crossover_trials`
- `use_tardiness_opportunity_dispatch`
- `prefer_sequence_crossover_dispatch`

这样后续 benchmark 可以直接配置这些机制。

### 8. 新增测试

新增或扩展测试包括：

- `test_sequence_crossover_neighborhood_improves_projected_order`
- `test_quota_marginal_service_ignores_redundant_on_time_quantity`
- `test_tardiness_opportunity_gain_values_late_pull_forward`

测试文件：

- `sl_isp_rg_rho_lns/tests/test_rho_projection.py`
- `sl_isp_rg_rho_lns/tests/test_metaheuristic_baselines.py`

完整测试结果：

```text
124 passed, 1 warning
```

## 代表算例结果

算例设置：

```text
jobs = 10
machines = 4
operations per job = 2-3
entities = 3
effective due values = [12, 20, 28]
exact time limit = 60s
```

对比结果：

| 算法 | Z | gap vs exact | TT | WSF |
|---|---:|---:|---:|---:|
| Exact | 12.93 | 0.00 | 9 | 3.93 |
| EDD | 81.35 | 68.42 | 39 | 42.35 |
| SPT | 61.43 | 48.50 | 37 | 24.43 |
| WSPT | 52.43 | 39.50 | 42 | 10.43 |
| ATC | 47.43 | 34.50 | 37 | 10.43 |
| SA-Fast | 36.43 | 23.50 | 30 | 6.43 |
| GA-Fast | 31.93 | 19.00 | 28 | 3.93 |
| TS-Fast | 35.93 | 23.00 | 32 | 3.93 |
| VNS-Fast | 35.93 | 23.00 | 32 | 3.93 |
| 旧主算法水平 | 35.93 | 23.00 | 32 | 3.93 |
| 新主算法 | 29.93 | 17.00 | 26 | 3.93 |

新主算法最终选择的 rollout 候选是：

```text
NR-RG-RHO-LNS-sequence-evolution
```

说明：

- 新主算法保持了和 GA 相同的 WSF；
- 同时将 TT 从 GA 的 28 降到 26；
- 在该算例上超过了 GA-Fast；
- gap 从旧主算法的 23.00 缩小到 17.00。

## 今日关键结论

### 1. GA 的优势可以被吸收，但不能照搬 GA

GA 的优势来自全局序列重组，不是种群框架本身。因此适合吸收到主算法中的形式是：

```text
sequence crossover neighborhood + rollout selection
```

而不是把完整 GA 当作另一个兜底算法。

### 2. 主算法原来的失败机制是边际收益判断不够精确

旧主算法容易将“属于高风险实体”误判为“必须准时保护”，导致一些服务冗余作业过早占用机器。

实际上，服务目标是 quota-based：

```text
shortfall = max(0, min_fulfillment - on_time_quantity)
```

超过 quota 的准时数量没有额外服务收益。

### 3. 迟交作业不能被过早放弃

J9 说明：

- 即使作业已经无法准时；
- 即使它不能减少 WSF；
- 只要提前完成能减少 TT，就应该获得一定优先级。

这就是 tardiness opportunity cost 的意义。

### 4. 最稳健的设计是候选生成 + no-regret 选择

直接强制执行 sequence crossover 或 tardiness pull-forward 都可能破坏服务结构。

因此最终采用：

```text
多个候选策略生成完整 rollout；
用真实目标 Z 选择最优候选；
不使用 exact 作为全局兜底。
```

这样既能发挥主算法优势，也能降低错误机制被放大的风险。

## 后续建议

1. 在 20-job、4-machine、4-entity 等更大规模算例上复测新主算法。
2. 记录 `sequence-evolution` 被选择的频率和贡献幅度，判断它是否具有普遍收益。
3. 对不同截止期复杂度、不同 rho 水平、不同 beta/alpha 设置做敏感性实验。
4. 在论文中将该机制表述为：

```text
quota-aware and tardiness-opportunity-cost guided sequence evolution
```

它可以作为主算法的一个创新模块，而不是外部元启发式补丁。

## 晚间补充优化：大规模 rollout 组合收缩

针对 40/80 工件测试中的现象，主算法做了两点针对性调整：

1. `sequence-evolution` 邻域默认只在 `num_jobs <= 30` 时进入 rollout portfolio。80 工件结果中该候选 Z 很差且单候选耗时约 7-8 分钟，因此大规模默认跳过它。
2. 完整 NR 内部候选默认也只在 `num_jobs <= 30` 时进入 rollout portfolio。40/80 工件已有结果显示主收益主要来自 EDD/ATC 等轻量候选选择，完整 NR 派生候选在大规模下没有贡献，放开后还会造成长时间搜索。

同时修正了一个退化判断：只有当 EDD 投影本身也不会造成服务短缺时，才允许主算法强制退化为 EDD；否则保留服务风险判断。

验证：

```text
python -m pytest
128 passed in 18.74s
```

当前建议：30 工件以内保留 sequence/neighborhood 强化以支撑主算法优势；40/80 工件以上采用轻量 rollout portfolio，先稳住运行时间和 no-regret 选择优势，再单独设计大规模服务短缺修补邻域。
